# Explanations of steps for level01

We remember the flag01 password in /etc/passwd, Google said us the program john can crack it.
Let download the passwd file
	scp -P 4242 level01@IP.OF.THE.VM:/etc/passwd ~/passwd_sc

let now install john and launch it on the passwd file
	john ~/passwd_sc

We see john cracked the pasword of flag01 : "abcdefg"
let su flag01

The token for level02 is : "f2av5il02puano7naaf6adaaf"
